import { IEnvironment } from './environment';

export const environmentProd: IEnvironment = {
    environments: [
        {
            name: '_name_',
            url: '_api_',
        },
    ],
    isLoggerEnabled: false,
};
